# Below you can include specific configuration files depending on what you want KAMP to do:

## Native Klipper adaptive meshing is used by PRINT_START. Do not include
## KAMP/Adaptive_Meshing.cfg, which replaces BED_MESH_CALIBRATE.
## The repository-owned tapered_line_purge.cfg replaces KAMP's Line_Purge.cfg.
## It always purges along Y, beginning at the rear and tapering to normal width.
[include tapered_line_purge.cfg]
#[include ./KAMP/Voron_Purge.cfg]            # Include to enable adaptive Voron logo purging configuration.
#[include ./KAMP/Smart_Park.cfg]             # Replaced by repository-owned REAR_PURGE_PARK.

[gcode_macro _KAMP_Settings]
description: This macro contains all adjustable settings for KAMP 

# The following variables are settings for KAMP as a whole.
variable_verbose_enable: True               # Set to True to enable KAMP information output when running. This is useful for debugging.

# The following variables are for adjusting adaptive mesh settings for KAMP.
variable_mesh_margin: 0                     # Expands the mesh size in millimeters if desired. Leave at 0 to disable.
variable_fuzz_amount: 0                     # Slightly randomizes mesh points to spread out wear from nozzle-based probes. Leave at 0 to disable.

# The following variables are for those with a dockable probe like Klicky, Euclid, etc.                 # ----------------  Attach Macro | Detach Macro
variable_probe_dock_enable: False           # Set to True to enable the usage of a dockable probe.      # ---------------------------------------------
variable_attach_macro: 'Attach_Probe'       # The macro that is used to attach the probe.               # Klicky Probe:   'Attach_Probe' | 'Dock_Probe'
variable_detach_macro: 'Dock_Probe'         # The macro that is used to store the probe.                # Euclid Probe:   'Deploy_Probe' | 'Stow_Probe'
                                                                                                        # Legacy Gcode:   'M401'         | 'M402'

# The following variables are for adjusting adaptive purge settings for KAMP.
variable_purge_height: 0.20                 # First-layer height used for the tapered purge.
variable_tip_distance: 4.0                  # Matches PRINT_END's 4 mm retract.
variable_purge_margin: 5.0                  # Clearance between the purge-line centre and the object.
variable_purge_line_length: 55.0            # Requested Y-axis purge length; clamped to the bed.
variable_purge_start_width: 1.60            # Wide rear end catches residue and builds pressure.
variable_purge_end_width: 0.45              # Normal-width front end conditions extrusion for printing.
variable_purge_speed: 25.0                  # Constant XY speed in mm/s through the taper.
variable_rear_prime_amount: 2.0             # Free-air purge after restoring the retracted tip.
variable_rear_park_height: 10.0             # Clearance above the rear edge while heating.
variable_purge_edge_clearance: 2.0          # Keep the deposited line inside the printable limits.

# The following variables are for adjusting the Smart Park feature for KAMP, which will park the printhead near the print area at a specified height.
variable_smart_park_height: 10              # Z position for Smart Park, default is 10.

gcode: # Gcode section left intentionally blank. Do not disturb.

    {action_respond_info(" Running the KAMP_Settings macro does nothing, it is only used for storing KAMP settings. ")}
